While coding, you should aim for explicitness over simplicity.  
  
It's a common perception that the simpler the code, the better, however, when looking for maintainability and readability you should always consider how explicit the code is.  
  
Below is an example of a token validation function and two different approaches.  
  
Even though you can easily tell what the first function does, it’s pretty hard to know what it is actually doing.

![[simple_explicit_code.jpg]]