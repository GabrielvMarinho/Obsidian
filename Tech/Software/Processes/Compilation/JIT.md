[[Compiler|Compile]] the [[Bytecode]] during [[Runtime]], compiles entire functions and execute each time.

Optmizes function calling and many other aspects with runtime information -> like how many times a func is called.