When you have a thread with a busy resource, you use a request mechanism to make ask for the resources to be realeased.

This is nice because a [[Thread]] can wait for another resource, but managing the request system is tricky