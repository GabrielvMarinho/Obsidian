- You setup a set of tests and test files.
    
- You push code to the `master` branch.
    
- This setup is triggered by the CI pipeline.
    
- The pipeline runs on a clean/empty [[Operating System]] to ensure consistency.
    
- The pipeline executes tests, compiles the code, and validates that everything works.