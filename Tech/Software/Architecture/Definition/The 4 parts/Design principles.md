Basically an [[Architecture decisions|architecture decision]], but rather than a static rule, its a guideline.

Example: the development teams should leverage asynchronous messaging between services within a microservices architecture to increase performance.