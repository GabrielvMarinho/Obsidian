Define the rules for how a system should be constructed.

Example: Only the business and service layers within a layered architecture can access the database

when that rule is broken -> its called a variance