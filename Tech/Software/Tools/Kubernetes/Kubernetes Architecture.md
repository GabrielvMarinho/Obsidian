[[Kubernetes]] env will have a cluster, a cluster will have many nodes, each node has many pods, each pod has a container, each container will have an app running, you can use services to expose those pods.


check [[Visual Maps\kubernetesArchitecture.excalidraw]]