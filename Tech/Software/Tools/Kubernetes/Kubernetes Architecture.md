[[Kubernetes]] env will have a cluster, a cluster will have many nodes, each node has many pods, each pod has a container, each container will have an app running, each app can and will start its own [[Web Server]], you can use services to expose those containers.


check [[kubernetesArchitecture.excalidraw]]