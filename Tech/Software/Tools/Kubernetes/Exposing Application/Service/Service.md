A network abstraction that defines a logical set of pods and how to access them (exposing IP and DNS or port, for example), the pods can and will die, thats why a service referentiate it through a [[Selector Label]].

A service has a [[Virtual IP]]
