components ->

- **Network Namespace Isolation**
    - Each virtual network provides isolated network stacks (IP addresses, routing tables, firewall rules) per container or VM.
    
- **Virtual IP Addressing**
    - Containers/VMs get their own virtual IPs that are unique within the virtual network.