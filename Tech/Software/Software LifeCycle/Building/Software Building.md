Depending on the [[Programming Language]] you can have different approaches for building software to be used by another machine.

 Some languages compile the code directly into [[Machine Code]] ant package it.

  Soma languages bundle the code itself and package the language [[Interpreter]] to just when the app is executed, the app will actually be translated to [[Machine Code]].
  
  Somewhere at some point, every language turns into [[Machine Code]]