

list images
```bash
docker images
```

list running containers
```
docker ps
```

running a container
```
docker run <image_name>
```

running containers attributes:

**-d** | detached
**-p <host_port>:<container_port>** | port mapping 