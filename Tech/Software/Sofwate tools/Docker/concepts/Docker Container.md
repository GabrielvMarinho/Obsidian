
A [[Containers|Container]] to run instance of a single [[Docker Image]], it will grab relevant information about the environment to be run for example

The containers share resources with the hosts computer kernel, if its a linux, if its a linux base image and a windows host, it will actually run a linux vm under the hood.


