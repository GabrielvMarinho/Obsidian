A [[CPU]] component used to schedule and deal with multiple processes running concurrently

It uses a [[Timer Interrupt]] to regain access over the cpu and uses scheduling algorihtms such as [[MLFQ]], [[Lottery scheduler]], [[Stride scheduler]], [[CFS]], etc.
