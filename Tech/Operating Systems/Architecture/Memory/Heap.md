Whenever you create dynamic objects, Strings, objects and so on, the data will be allocated in the heap

- Heap stores memory for objects created at runtime
- Slower access because of pointer dereferencing
- Can persist outside of the scope it was created