instead of mapping a [[Virtual memory]] byte to a [[Physical memory]] byte, they divide the memory in chunks, to increase efficiency, this page number is usually 4KB on 64 bit OS

virtual memory: pages
physical memory: frames

a page is linked to a frame