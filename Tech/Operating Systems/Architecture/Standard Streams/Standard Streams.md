preconnected input and output communication channels between a computer program and its environment when it begins execution
the three I/O connections are called [[Standard input]], [[Standard output]], and [[Standard error]]

when you type ls in the terminal it goes to the input stream of the terminal, the result is sent to the standard output.