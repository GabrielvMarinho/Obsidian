a [[Mathematics|mathematical]] function to turn any value into a fixed size output, also know as [[Hash]]

a hash function should always produce different and unique outputs for each input, same value, same output

a hash function should be fast

a hash function should be secure, seemingly impossible to determine the input based on a hash value, a small difference generates completely different hash