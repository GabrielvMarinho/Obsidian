A [[Data Structure]] that is composed of nodes, each node has maximum two children, the left child and the right child.

Sorted in a way that everything on the left is smaller than the current node and everything on the right is higher than the current node.

Uses [[Binary Search]] algorithm
