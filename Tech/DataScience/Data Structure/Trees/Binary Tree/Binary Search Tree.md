A [[Data Structure]] that represents a tree of nodes, each node has maximum two children, the left child and the right child.

Sorted in a way that everything on the left is smaller than the current node and everything on the right is higher than the current node.

To have a balanced Binary Search Tree, you use the median of the elements to be the root node

Uses [[Binary Search]] algorithm
