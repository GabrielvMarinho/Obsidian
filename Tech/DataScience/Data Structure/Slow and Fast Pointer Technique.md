You half a slow pointer, which will move one position at a time.
You half a fast pointer, which will move two positions at a time.

When fast pointer finds the end of the structure, slow pointer will be right in the middle.
