1. an [[HTTP Client]] searches for an url
2. [[DNS]] returns the [[IP]] address related to that url
3. The connection is established by [[TCP]] by doing a [[Three way handshake]] (if using https it will use [[SSL]])