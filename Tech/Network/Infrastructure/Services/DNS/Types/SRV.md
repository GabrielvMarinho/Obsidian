[[DNS]] service record especifies a host and port for a specific service.

Record stores:
1. domain name
2. protocol
3. port number
4. and more

It DOESN'T create a new url, just mapping.

Example:
   _service._protocol.domain.com → hostname:port
