A component that has a state and implements a kind of behaviour.

An object abstract the manipulation of the state through the behaviour.

A component that freely gives access to its state without implementing no kind of behaviour, is not an object, rather, a [[Data Structure]]