a [[NodeJS]] api for building addons, independent of [[V8]] (or any jasvascript runtime).

N-API provides the possibility to create c/c++ addons that are compatible with other nodejs version, to avoid having to recompile the code.