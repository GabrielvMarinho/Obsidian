Can store types of values with a variety of types

```rust
let tup: (i32, f64, u8) = (500, 6.4, 1)
```


simple tuple handling with rust
```rust
let x: (i32, f64, u8) = (500, 6.4, 1); let five_hundred = x.0;
let six_point_four = x.1;
let one = x.2;
```