1. Sequest to HTML shell
2. Server Resoibds with javascript bundles
3. Browser parses and executes javascript
4. Client fectches APIs data after js execution
5. UI is updated with data

## Nice to notice

Everything is js, no matter if you use Angular, React, Vue, they all become js in the end because this is what the browser understands.