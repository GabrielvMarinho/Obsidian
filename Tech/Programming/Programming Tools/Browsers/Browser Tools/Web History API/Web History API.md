built in [[Browser]] [[API]] for handling history of the endpoint access, every browser has to implement it independently but they are all based on an existing pattern/consensus

it can be accessed by all browsers in window.history