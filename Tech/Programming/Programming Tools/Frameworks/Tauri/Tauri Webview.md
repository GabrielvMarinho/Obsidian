[[Tauri]] frontend uses [[WKWebView]] on mac and linux, [[WebView2]] on windows to display web content easily in the desktop and "simulates" a browser

It basically gets a technology that wraps browsers to run them and process webpages, other functionalities and all natively depending on the OS