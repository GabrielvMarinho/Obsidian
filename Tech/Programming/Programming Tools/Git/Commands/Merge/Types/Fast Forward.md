When just the pointer of the branch is moved.
this behaviour happens when you have one commit only to merge.   

Example:


A---B---C   (main)
         \
          D---E---F   (feature)

being in main and merging with feature will just move the master pointer to ``F``