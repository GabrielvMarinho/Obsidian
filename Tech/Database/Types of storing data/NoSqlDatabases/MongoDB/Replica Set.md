A group of [[MongoDB]] instances with the same data set, updating all the instances when something changes, creating a highly available environment.

The most optimal scenario for production deployed application is to have each replica set in a computer .

The centralization of the management process of these replicas is done generally by [[MongoDB Ops Manager]].