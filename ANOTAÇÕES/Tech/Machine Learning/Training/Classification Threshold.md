Is basically how confident we want the [[Machine Learning]] model to classify an example as positive or false

We can tell the model to follow a Threshold of 0.6, if we have an example that has a 0.55 chance of being positive, it will not be labeled as positive, only if ever over 0.6
