Implements a behaviour to a class, while you would not make "Bird" or "Plane" from the same class, you could make it implements the same behaviour from an interface like "Flyer"

While [[Heritage]] lets your create similar composition, interfaces are for behavior only 