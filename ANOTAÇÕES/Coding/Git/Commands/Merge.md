Merge combines changes from one branch (source branch) into another branch (target branch). Don't confuse with [[Rebase]], Similar but different.

to merge two branches:
```
git merge "nomeBranch"
```